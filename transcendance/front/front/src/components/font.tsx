import { Agbalumo, Rubik } from 'next/font/google';

export const agbalumo = Agbalumo({
    weight: ['400'],
    subsets: ['latin']
});

export const rubik = Rubik({
    weight: ['400'],
    subsets: ['latin']
});